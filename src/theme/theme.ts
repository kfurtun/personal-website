import { Theme } from 'src/models/theme';

const theme: Theme = {
  colors: {
    blue1: '#4550FF',
    white1: '#FFFFFF',
    white2: '#F3F6FF',
    black1: '#000000',
  },
};

export default theme;
